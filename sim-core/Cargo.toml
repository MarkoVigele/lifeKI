[package]
name = "anima_core"
version = "0.1.0"
edition = "2021"
description = "WASM heart of lifeKI — neural particle life"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "=0.2.100"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
